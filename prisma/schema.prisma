generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id         String   @id @default(uuid())
  name       String?
  cpf        String?  @unique
  birthday   DateTime
  email      String   @unique
  created_at DateTime @default(now())
  Cart       Cart[]
  active     Boolean

  @@map("users")
}

model Product {
  id          String   @id @default(uuid())
  price       Float?
  description String?
  category    String?
  created_at  DateTime @default(now())
  Cart        Cart[]
  active      Boolean

  @@map("products")
}

model Cart {
  id         String   @id @default(uuid())
  product_id String
  product    Product? @relation(fields: [product_id], references: [id])
  user_id    String
  user       User?    @relation(fields: [user_id], references: [id])
  active     Boolean

  @@map("carts")
}

// Criar tabelas Order e Category
// Ajeiter nome Preco para Price
// Busca por Category
// Histórico de Order 
